/**
 * install jsonwebtoken
 * jwt.sign (payload, secret, {expiresIn:})
 * token client
 * **/ 
/**
 * hoe to store token in the client side
 * memory --> ok type
 * local storage -->ok type(X55)
 * cookies: http only
 * install: npm install cookie-parser
 * */ 

/**
 *set cookies with http only. for development secure: false
 *cors
 * app.use(cors({
    origin:['http://localhost:5173'],
    credentials:true
})); 
 * */ 